# idea5_idle

一存档多名 worker：派遣去生活挂机或战斗，账号共用银行和金币。战斗技能用金币学；伐木 / 采矿 / 炼金是另一套生活玩法。

## 安装

```bash
npm install
```

## 命令

```bash
npm run dev
npm run build
npm test
```

开发页默认 http://localhost:5173 。存档写在 `localStorage`，键名 **`idea5Idle`**。

测试也可以直接：

```bash
npx vitest run
```
